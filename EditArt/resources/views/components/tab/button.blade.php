
<button class="nav-link" id="{{$id}}" data-bs-toggle="{{$toggle}}" data-bs-target="{{$target}}" type="button" role="tab" aria-controls="{{$controls}}" aria-selected="{{$select}}">
    {{$slot}}
</button>
